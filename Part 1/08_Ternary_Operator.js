/* 
TERNARY OPERATOR / CONDITIONAL OPERATOR
    > syntax
        condition ? 'value_if_true' : 'value_if_false'
*/
// The ternary operator is a shorthand way to write an if-else statement. It has the following syntax: condition ? expr1

let age = 15;
let drink = age>5? 'tea' : 'milk';

console.log(drink);